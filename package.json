{
  "name": "dodao-ui",
  "private": true,
  "workspaces": [
    "academy-ui",
    "referral-ui",
    "base-ui",
    "defi-alerts",
    "x-news-ui",
    "insights-ui",
    "clickable-demos",
    "news-reader",
    "simulations",
    "shared/web-core",
    "dodao-lambdas/tweet-collection-updater"
  ],
  "scripts": {
    "build:web-core": "cd shared/web-core && yarn build",
    "build:academy": "cd academy-ui && yarn build",
    "build:referral": "cd referral-ui && yarn build",
    "build:base": "cd base-ui && yarn build",
    "build:defi-alerts": "cd defi-alerts && yarn build",
    "build:x-news": "cd x-news-ui && yarn build",
    "build:insights": "cd insights-ui && yarn build",
    "build:clickable-demos": "cd clickable-demos && yarn build",
    "build:news-reader": "cd news-reader && yarn build",
    "build:simulations": "cd simulations && yarn build",
    "build:tweet-updater": "cd dodao-lambdas/tweet-collection-updater && yarn build",
    "build:all": "yarn build:web-core && yarn build:academy && yarn build:referral && yarn build:base && yarn build:defi-alerts && yarn build:x-news && yarn build:insights && yarn build:clickable-demos && yarn build:news-reader && yarn build:simulations && yarn build:tweet-updater"
  },
  "resolutions": {
    "@types/react": "18.3.12",
    "@types/react-dom": "18.3.1",
    "react": "18.3.1",
    "react-dom": "18.3.1"
  }
}
