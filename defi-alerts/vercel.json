{
  "headers": [
    {
      "source": "/api/(.*)",
      "headers": [
        { "key": "Access-Control-Allow-Credentials", "value": "true" },
        { "key": "Access-Control-Allow-Origin", "value": "*" },
        { "key": "Access-Control-Allow-Methods", "value": "GET,OPTIONS,PATCH,DELETE,POST,PUT" },
        { "key": "Access-Control-Allow-Headers", "value": "X-CSRF-Token, X-Requested-With, Accept, Accept-Version, Content-Length, Content-MD5, Content-Type, Date, X-Api-Version" }
      ]
    }
  ],
  "buildCommand": "cd shared/web-core && yarn install --ignore-workspace-root-check && yarn build && cd ../../defi-alerts && yarn install --ignore-workspace-root-check && prisma generate && next build",

  "crons": [
    {
      "path": "/api/cron",
      "schedule": "0 * * * *"
    },
    {
      "path": "/api/sending-alerts/compare-compound",
      "schedule": "0 * * * *"
    },
    {
      "path": "/api/sending-alerts/compound-market-alerts",
      "schedule": "0 * * * *"
    }
  ]
}
