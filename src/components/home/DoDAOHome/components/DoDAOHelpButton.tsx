'use client';
import HelpButton from 'tidbits-help-icon';

export default function DoDAOHelpButton() {
  const url = 'https://dodao-tidbits.tidbitshub.org/embedded-tidbit-collections';
  return HelpButton(url);
}
