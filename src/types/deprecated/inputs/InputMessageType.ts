export enum InputMessageType {
  guide = 'guide',
  proposal = 'proposal',
  settings = 'settings',
  follow = 'follow',
  unfollow = 'unfollow',
  alias = 'alias',
  subscribe = 'subscribe',
  unsubscribe = 'unsubscribe',
}
