mutation AddDiscordCredentials(
  $spaceId: String!
  $code: String!
  $redirectUri: String!
) {
  payload: addDiscordCredentials(
    spaceId: $spaceId
    code: $code
    redirectUri: $redirectUri
  ) {
    id
  }
}
