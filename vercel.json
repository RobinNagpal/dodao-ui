{
  "buildCommand": "cd shared/web-core && yarn install && yarn build && cd ../../defi-alerts && yarn install && prisma generate && next build",
  "outputDirectory": "defi-alerts/.next",
  "rootDirectory": "defi-alerts",
  "installCommand": "cd shared/web-core && yarn install && cd ../../defi-alerts && yarn install"
}
